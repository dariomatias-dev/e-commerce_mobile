import 'package:flutter/material.dart';

class BottomNavigationProductWidget extends StatelessWidget {
  const BottomNavigationProductWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}